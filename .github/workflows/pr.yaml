on:
    pull_request:
        branches:
            - main
        types: [opened, synchronize, reopened]

jobs:
    pr-enforce-cc:
        runs-on: ubuntu-latest
        permissions:
            contents: read
            pull-requests: read

        steps:
            - name: "Checkout repository"
              uses: actions/checkout@v4

            - name: "Validate conventional commit messages"
              uses: Alirezaja1384/Summer1403-Project-Group02-Deployment/.github/actions/pr-conventional-commit
              with:
                  token: ${{ secrets.GITHUB_TOKEN }}
